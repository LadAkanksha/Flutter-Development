void main() {
  var differ = "Flutter";
  print(differ);
  print(differ.runtimeType);

  differ = "Android";
  print(differ);
  print(differ.runtimeType);

  //differ = 121.67;
  /*A value of type 'double' can't be assigned to a variable of type 'String'.
 Try changing the type of the variable, or casting the right-hand type to 'String'.*/

  /*
  In Var type, Once the type is initialized, It remains fixed and can't altered.
  */
}
